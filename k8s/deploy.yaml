---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: posenet-hub
  namespace: posenet
  labels:
    app: posenet-hub
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: posenet-hub
    spec:
      containers:
        - name: posenet-hub
          image: "gitlab-registry.nautilus.optiputer.net/librareome/posenet/posenet-hub:$CI_COMMIT_SHORT_SHA"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50051
            - containerPort: 3083
